For license reason, it can't be build in CI.

So if your need support for some platform, please run:

```shell
cargo build --release
```

to get the binary and upload here.

The directory name should be same as `$SystemID`.
